name: CI/CD Pipeline

on: [push]

jobs:
  build:
  runs-on: ubuntu-latest
  steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
        
    - name: Build and push Payment Service
      uses: docker/build-push-action@v6
      with:
        context: ./payment-service
        file: ./payment-service/Dockerfile
        push: true
        tags: denchiquero/payment-service:latest


  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Unit Tests
        run: |
          echo "test"

  integration-tests:
    needs: unit-tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Integration Tests
        run: |
          # Тестируем взаимодействие между 2-3 сервисами
          echo "Testing payment + order integration..."
          # Здесь тесты, которые проверяют API вызовы между сервисами

  e2e-tests:
    needs: integration-tests  
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run E2E Tests
        run: |
          # Тестируем ВСЮ систему
          docker swarm init
          docker stack deploy -c docker-compose.yml app
          sleep 30
          # Тесты, которые проходят полный сценарий:
          # Создание заказа → Оплата → Получение чека